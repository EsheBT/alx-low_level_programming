#include "main.h"

/**
 * _isdigit - a function that check for digits
 * @j:input
 * Return: 1 if j is digit , 0 otherwise
 */
int _isdigit(int j)
{
	if ((j >= '0' && (j <= '9'))
			return (1);
			else
			return (0);
			}
