#include "main.h"

/**
 * _isupper - checks for uppercase 
 * @j: input
 * Return: 1 if j is uppercase, 0 otherwise
 */
int _isupper(int j)
{
	if ((c >= 'A') && (c <= 'Z'))
		return (1);
	else
		return (0);
}
