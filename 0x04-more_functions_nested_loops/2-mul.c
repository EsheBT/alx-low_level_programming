#include "main.h"

/**
 * mul - is a function for two number multiplication
 * @a:input
 * @b:input
 * Return:always return result
 */
int mul(int a, int b);
{
m = 0;
m = a * b;
printf("%d", m)
return (0);
}
