#include <stdio.h>

/**
 * main - The primary function
 *
 *
 * Return: end the function execusion
 */

int main(void)
{
	char m;

	for (m = 'z' ; m >= 'a' ; m--)
	{
	putchar(m);

	}
	putchar('\n');

	return (0);
}
